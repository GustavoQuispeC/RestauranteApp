@page "/tipopagos/list"
@using RestauranteApp.Shared
@inject HttpClient Http
@inject NavigationManager NavigationManager

<h3>Tipo de Pago</h3>
<hr />
<a href="tipopagos/crear" class="btn btn-success">Nuevo <Icon Name="IconName.PinAngleFill" /></a>
<br />
<br />


@if (_TipoPago == null)
{
    <p><em>Cargando...</em></p>
}
else
{
    <table class="table table-hover table-bordered table-striped">
        <thead class="color-font">
            <tr>
                <th>Id:</th>
                <th>Descripcion:</th>
                <th>Observacion:</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in _TipoPago)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Nombre</td>
                    <td>@item.Observacion</td>
                    <td>
                        <Button Class="colorEditar" @onclick="()=>Editar(item.Id)"><Icon Name="IconName.PenFill" /></Button>
                        <Button Class="colorEliminar" @onclick="()=>Eliminar(item.Id)"><Icon Name="IconName.TrashFill" /></Button>
                    </td>
                </tr>
            }
        </tbody>
    </table>

}


@code {
    private TipoPagoDto[]? _TipoPago;
    protected override async Task OnInitializedAsync()
    {
        await Cargar();
    }
    private async Task Cargar()
    {
        _TipoPago = await Http.GetFromJsonAsync<TipoPagoDto[]>("api/tipopagos");
    }
    private void Editar(int id)
    {
        NavigationManager.NavigateTo($"tipopagos/editar/{id}");
    }
    private async void Eliminar(int id)
    {
        await Http.DeleteAsync($"api/tipopagos/{id}");
        await Cargar();
    }
}
